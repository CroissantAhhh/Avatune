


export default function SearchResultsPage() {

    // Track Results + See all ->

    // Album Results + See all ->

    // Medium Results + See all ->

    // Artist Results + See all ->

    // Playlist Results + See all ->

    // User Results + See all ->

    return (
        <p>Search Results Page</p>
    )
}
